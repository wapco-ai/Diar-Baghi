name: diar-baghi

services:
  citizen-web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - citizen_web_node_modules:/app/node_modules
    stdin_open: true
    tty: true

volumes:
  citizen_web_node_modules:
